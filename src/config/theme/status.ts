import { colors } from '@mui/material'

const status = {
  primary: {
    normal: 'white',
    danger: colors.red[500]
  },
  secondary: {
    normal: 'red',
    danger: colors.blue[500]
  }
}

export default status
