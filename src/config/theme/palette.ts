import { colors } from '@mui/material'

const palette = {
  // Palette key
  primary: {
    // Palette Options
    main: colors.orange[500],
    sub: colors.orange[50]
  },
  secondary: {
    main: colors.purple[500],
    sub: colors.purple[50]
  },
  testValue: 'red'
}

export default palette
